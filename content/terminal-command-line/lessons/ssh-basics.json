{
  "slug": "ssh-basics",
  "title": "SSH: Connecting to Remote Servers",
  "type": "incident_response",
  "difficulty": 2,
  "xpReward": 15,
  "unitSlug": "power-user-techniques",
  "courseSlug": "terminal-command-line",
  "intro": "Your app is live on a remote server and a critical bug just hit production. You need to connect to the server, check the logs, and restart the service -- all through the terminal. SSH is the secure tunnel that lets you control a remote machine as if you were sitting right in front of it.",
  "teach": [
    {
      "type": "analogy",
      "title": "SSH = A Secure Private Phone Line",
      "body": "Imagine you need to give instructions to someone in a locked building across town. You could shout across the street (insecure -- anyone can hear), or you could use a **private, encrypted phone line** that only you and the person inside can use. **SSH is that private phone line.** It creates an encrypted tunnel between your computer and a remote server so nobody can eavesdrop on your commands or data."
    },
    {
      "type": "analogy",
      "title": "SSH Keys = A Fingerprint Scanner",
      "body": "Most buildings use keys or passcodes to get in -- but those can be copied or guessed. A **fingerprint scanner** is more secure because your fingerprint is unique and can't be stolen by watching over your shoulder. **SSH keys work like a fingerprint scanner.** Instead of typing a password every time (which could be intercepted), you register your unique key with the server. When you connect, it checks your fingerprint -- no password needed, and nothing to steal."
    },
    {
      "type": "text",
      "title": "SSH Essentials",
      "body": "SSH (Secure Shell) lets you run commands on a remote machine:\n\n- **`ssh user@hostname`** -- Connect to a remote server\n- **`ssh -i key.pem user@hostname`** -- Connect using a specific key file\n- **`ssh-keygen`** -- Generate a new SSH key pair (public + private)\n- **`scp file.txt user@host:/path/`** -- Copy a file to a remote server\n- **`exit`** -- Disconnect from the remote server\n\nThe private key stays on your machine (never share it). The public key goes on the server."
    },
    {
      "type": "code_example",
      "title": "SSH in Practice",
      "language": "bash",
      "code": "# Generate an SSH key pair\nssh-keygen -t ed25519 -C \"you@email.com\"\n\n# Connect to a server\nssh deploy@192.168.1.100\n\n# Run a command on the server without staying connected\nssh deploy@192.168.1.100 'pm2 restart all'\n\n# Copy a file to the server\nscp dist.zip deploy@192.168.1.100:/var/www/app/",
      "annotation": "ssh-keygen creates two files: a private key (keep secret) and a public key (.pub -- share this with servers). The ed25519 type is the modern recommended algorithm."
    },
    {
      "type": "motivation",
      "body": "Every production app lives on a remote server. When clients pay you to build and maintain apps, SSH is how you deploy updates, debug production issues, and manage servers. Vibecoders who can SSH into a server and fix issues in minutes earn premium rates -- because downtime costs businesses real money."
    },
    {
      "type": "key_takeaway",
      "body": "SSH creates an encrypted connection to remote servers. Use ssh-keygen to create key pairs (more secure than passwords), ssh user@host to connect, and scp to copy files. Never share your private key -- only the public key goes on the server."
    }
  ],
  "exercise": {
    "startNode": "start",
    "nodes": {
      "start": {
        "narrative": "Your client calls in a panic -- their web app is down. You need to SSH into the production server at deploy@app.example.com to investigate. You try to connect but get 'Permission denied (publickey)'. What do you do first?",
        "choices": [
          { "id": "a", "text": "Check if your SSH key is added to the ssh-agent with ssh-add -l", "nextNode": "check-agent", "score": 4 },
          { "id": "b", "text": "Generate a brand new SSH key pair with ssh-keygen", "nextNode": "new-key", "score": 0 },
          { "id": "c", "text": "Try connecting with a password instead: ssh deploy@app.example.com -o PasswordAuthentication=yes", "nextNode": "try-password", "score": 1 }
        ]
      },
      "check-agent": {
        "narrative": "You run ssh-add -l and get 'The agent has no identities'. Your SSH key exists on disk but isn't loaded into the agent. You add it with ssh-add ~/.ssh/id_ed25519 and try connecting again. This time you get in. The server's terminal prompt appears. Now what?",
        "choices": [
          { "id": "a", "text": "Check the application logs to see what went wrong", "nextNode": "diagnose-on-server", "score": 4 },
          { "id": "b", "text": "Immediately restart the application service", "nextNode": "diagnose-on-server", "score": 1 },
          { "id": "c", "text": "Run top to check if the server is overloaded", "nextNode": "diagnose-on-server", "score": 2 }
        ]
      },
      "new-key": {
        "narrative": "You generate a new key, but the server doesn't recognize it -- you'd need to add the new public key to the server's authorized_keys file, which you can't do because you can't connect. Generating a new key doesn't help when the existing key just needs to be loaded.",
        "choices": [
          { "id": "a", "text": "Roll back the new key and check whether the existing key is loaded in the agent", "nextNode": "diagnose-on-server", "score": 2 }
        ]
      },
      "try-password": {
        "narrative": "The server has password authentication disabled for security (which is a best practice). Password-based SSH is vulnerable to brute-force attacks. The issue is with your SSH key, not the authentication method.",
        "choices": [
          { "id": "a", "text": "Add the existing key to the agent and try again", "nextNode": "diagnose-on-server", "score": 2 }
        ]
      },
      "diagnose-on-server": {
        "narrative": "You're connected. The app is down. Before you touch anything, you need to know WHY it's down. What's your diagnostic move on the server?",
        "choices": [
          { "id": "a", "text": "Tail the app's error log: `tail -100 /var/log/app/error.log` and `df -h` for disk usage", "nextNode": "fix-root-cause", "score": 4 },
          { "id": "b", "text": "Run `pm2 status` and `journalctl -u app -n 100` to see process state and system logs", "nextNode": "fix-root-cause", "score": 4 },
          { "id": "c", "text": "Skip diagnosis and run `pm2 restart all` -- the app is down, just turn it back on", "nextNode": "fix-root-cause", "score": 1 }
        ]
      },
      "fix-root-cause": {
        "narrative": "Diagnosis points to disk pressure: the app's log directory has ballooned to 50GB and the partition is at 100%. Writes to the log file fail, the app catches the I/O error, and pm2 keeps restarting it in a loop. What do you do?",
        "choices": [
          { "id": "a", "text": "Free disk space safely: `find /var/log/app -name \"*.log\" -mtime +30 -delete`, then `pm2 restart all` and confirm it's serving traffic", "nextNode": "prevent-recurrence", "score": 4 },
          { "id": "b", "text": "Run `rm -rf /var/log/app/*` to instantly free space, then restart", "nextNode": "prevent-recurrence", "score": 2 },
          { "id": "c", "text": "Just `pm2 restart all` -- maybe disk usage will sort itself out", "nextNode": "prevent-recurrence", "score": 0 }
        ]
      },
      "prevent-recurrence": {
        "narrative": "The app is back up. But the same disk-fills-up failure mode will return in roughly the same number of weeks. You're connected over SSH right now -- what's the right preventative move before you log out?",
        "choices": [
          { "id": "a", "text": "Configure logrotate (daily rotation, gzip, 14-day retention) and add a disk-usage alert in your monitoring", "nextNode": "end-good", "score": 4 },
          { "id": "b", "text": "Add a cron job that runs the find/-delete cleanup nightly", "nextNode": "end-ok", "score": 2 },
          { "id": "c", "text": "Note 'check disk usage weekly' in your calendar and log out", "nextNode": "end-bad", "score": 0 }
        ]
      },
      "end-good": {
        "narrative": "logrotate caps the log directory at a sane size automatically. The monitoring alert will page you at 80% disk usage long before the partition fills. The same incident cannot recur silently the way it just did. Total downtime: under 10 minutes.",
        "choices": [],
        "isEnd": true,
        "finalScore": 20,
        "feedback": "Production-grade SSH incident response. You diagnosed the auth issue locally, connected, gathered evidence before touching state, fixed the actual root cause (not the restart loop symptom), and installed both a control (logrotate) and a signal (alerting) so the failure mode is closed. That's the difference between a hero fix and a fix that holds."
      },
      "end-ok": {
        "narrative": "The cron cleanup will keep the partition from filling at the same rate, but it has no signal -- if cron fails or logs grow faster than expected, the next time you'll know is when the app crashes again.",
        "choices": [],
        "isEnd": true,
        "finalScore": 13,
        "feedback": "Better than nothing, but a cleanup job without alerting is a control without a signal. Pair it with logrotate (so growth is bounded by policy, not a script) and a disk-usage alert (so you're warned before it crashes again). Otherwise the same incident recurs the day cron silently fails."
      },
      "end-bad": {
        "narrative": "Three weeks later the partition fills again, the app crash-loops, the client calls in another panic, and you're SSHing into the same server to delete the same logs. Calendar reminders don't survive contact with a busy week.",
        "choices": [],
        "isEnd": true,
        "finalScore": 5,
        "feedback": "Manual reminders are not infrastructure. The only reliable fix for a class of failures is to remove the failure mode (logrotate caps growth) and add a signal that catches drift (a disk-usage alert). Otherwise you're paying with downtime every few weeks."
      }
    },
    "glossaryTerms": ["ssh", "ssh-key"]
  }
}
